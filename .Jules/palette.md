## 2024-05-22 - List Action Accessibility
**Learning:** Repetitive actions in a list (like "Open Case") confuse screen reader users if they don't have unique accessible names.
**Action:** Always append a unique identifier (like Case Code) to the `aria-label` of generic action buttons in a list.
