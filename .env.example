# Database Connection (Required for production, optional for demo mode)
# Get this from your PostgreSQL provider (Vercel Postgres, Neon, Railway, Supabase, etc.)
DATABASE_URL="postgresql://username:password@host:5432/database?schema=public"

# LLM API Keys (At least one is required for AI analysis)
# The system will use Gemini if available, otherwise falls back to OpenAI

# Google Gemini API Key (Recommended - Primary)
# Get it from: https://aistudio.google.com/app/apikey
# Supports: GOOGLE_API_KEY, GEMINI_API_KEY, or GOOGLE_GEMINI_API_KEY
GOOGLE_API_KEY="your-google-api-key"

# OpenAI API Key (Alternative/Fallback)
# Get it from: https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-..."
